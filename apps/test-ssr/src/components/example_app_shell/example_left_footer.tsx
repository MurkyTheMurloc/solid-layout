import type { Component } from "solid-js";

export const ExampleLeftFooter: Component = function () {
    return (
        <div
            style={{

                "text-align": "center",
                 border: "1px solid black",

            }}
        >
            Footer area left
        </div>
    );
};
